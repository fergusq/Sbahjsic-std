target("1.1.0")

setHelp(ArrayCreate, "Returns an array that contains the arguments.")
native ArrayCreate

setHelp(sort, "Sorts its argument array.")
native sort

setHelp(len, "Returns the length of its argument.")
native len